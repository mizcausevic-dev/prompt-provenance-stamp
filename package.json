{
  "name": "prompt-provenance-stamp",
  "version": "0.1.0",
  "description": "Generate a prompt-provenance-spec v0.1 document from raw prompt text. Computes sha256 hash, fills required fields, threads parent → lineage for derivation tracking, validates id/semver. Library + CLI.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "author": "mizcausevic-dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/mizcausevic-dev/prompt-provenance-stamp"
  },
  "keywords": [
    "prompt-engineering",
    "provenance",
    "ai-governance",
    "evidence",
    "sha256",
    "lineage",
    "typescript"
  ],
  "bin": {
    "prompt-provenance-stamp": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "demo": "tsx src/cli.ts fixtures/sample-prompt.md --id procurement-assistant --name procurement-assistant --version 1.0.0 --content-uri https://example.com/prompts/p/v1.md --created-by miz",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.2.7",
    "eslint": "^9.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^3.2.7"
  }
}
